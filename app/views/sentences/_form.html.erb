<%= form_with(model: sentence, local: true) do |form| %>
  <% if sentence.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sentence.errors.count, "error") %> prohibited this sentence from being saved:</h2>

      <ul>
      <% sentence.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :type %>
    <%= form.text_field :type, id: :sentence_type %>
  </div>

  <div class="field">
    <%= form.label :sentence %>
    <%= form.text_area :sentence, id: :sentence_sentence %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
