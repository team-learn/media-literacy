<%= form_with(model: number, local: true) do |form| %>
  <% if number.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(number.errors.count, "error") %> prohibited this number from being saved:</h2>

      <ul>
      <% number.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :sentence %>
    <%= form.text_field :sentence, id: :number_sentence %>
  </div>

  <div class="field">
    <%= form.label :sentence_id %>
    <%= form.number_field :sentence_id, id: :number_sentence_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
