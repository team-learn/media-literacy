<p id="notice"><%= notice %></p>

<h1>Entertainments</h1>

<table>
  <thead>
    <tr>
      <th>Sentence</th>
      <th>Sentence</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @entertainments.each do |entertainment| %>
      <tr>
        <td><%= entertainment.sentence %></td>
        <td><%= entertainment.sentence_id %></td>
        <td><%= link_to 'Show', entertainment %></td>
        <td><%= link_to 'Edit', edit_entertainment_path(entertainment) %></td>
        <td><%= link_to 'Destroy', entertainment, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Entertainment', new_entertainment_path %>
